{
  "name": "@podcast-crm/api",
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "fastify": "^5.8.5",
    "@fastify/cors": "^9.0.0",
    "@fastify/rate-limit": "^9.0.0",
    "@fastify/swagger": "^8.14.0",
    "@fastify/swagger-ui": "^6.1.1",
    "@fastify/jwt": "^8.0.0",
    "zod": "^3.22.0",
    "@podcast-crm/types": "workspace:*",
    "@podcast-crm/config": "workspace:*",
    "@podcast-crm/db": "workspace:*",
    "@podcast-crm/ai": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7",
    "@types/node": "^20.0.0"
  }
}
